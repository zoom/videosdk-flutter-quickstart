name: jwt_generator
description: JWT token generator for VideoSDK
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  args: ^2.4.2
  dart_jsonwebtoken: ^2.13.0
  dotenv: ^4.2.0
